import { Meta, Canvas } from '@storybook/blocks';
import { Spec } from './spec/Spec';
import * as ButtonStories from './Button.stories';
import manifest from '../../../packages/components/src/Button/Button.manifest.json';

<Meta title="Buttons/Button/Spec" />

export const AnatomyDiagram = () => (
  <div
    style={{
      display: 'inline-flex',
      alignItems: 'center',
      gap: 24,
      padding: '16px 24px',
      border: '2px dashed #94a3b8',
      borderRadius: 8,
      background: '#fff',
    }}
  >
    <div style={{ textAlign: 'center' }}>
      <div
        style={{
          width: 20,
          height: 20,
          background: '#dbeafe',
          border: '1px solid #3b82f6',
          borderRadius: 4,
          margin: '0 auto 6px',
        }}
      />
      <div style={{ fontSize: 11, color: '#3b82f6', fontFamily: 'ui-monospace, monospace' }}>
        leftIcon
      </div>
    </div>
    <div style={{ textAlign: 'center' }}>
      <div
        style={{
          padding: '4px 12px',
          background: '#dcfce7',
          border: '1px solid #22c55e',
          borderRadius: 4,
          fontSize: 13,
          fontWeight: 500,
          marginBottom: 6,
        }}
      >
        Label
      </div>
      <div style={{ fontSize: 11, color: '#22c55e', fontFamily: 'ui-monospace, monospace' }}>
        children
      </div>
    </div>
    <div style={{ textAlign: 'center' }}>
      <div
        style={{
          width: 20,
          height: 20,
          background: '#fef3c7',
          border: '1px solid #f59e0b',
          borderRadius: 4,
          margin: '0 auto 6px',
        }}
      />
      <div style={{ fontSize: 11, color: '#f59e0b', fontFamily: 'ui-monospace, monospace' }}>
        rightIcon
      </div>
    </div>
  </div>
);

<Spec
  manifest={manifest}
  anatomyDiagram={<AnatomyDiagram />}
  examples={
    <>
      <Canvas of={ButtonStories.Primary} />
      <Canvas of={ButtonStories.Secondary} />
      <Canvas of={ButtonStories.Tertiary} />
      <Canvas of={ButtonStories.DangerPrimary} />
      <Canvas of={ButtonStories.Disabled} />
      <Canvas of={ButtonStories.Loading} />
    </>
  }
  visualDos={[
    {
      label: 'One primary button per section, paired with a tertiary cancel.',
      example: <Canvas of={ButtonStories.DoPrimaryWithCancel} />,
    },
    {
      label: 'Use dangerPrimary for destructive actions.',
      example: <Canvas of={ButtonStories.DoDangerForDestructive} />,
    },
  ]}
  visualDonts={[
    {
      label: 'Never use two primary buttons in the same group.',
      example: <Canvas of={ButtonStories.DontTwoPrimaries} />,
    },
    {
      label: "Don't use dangerPrimary for non-destructive actions.",
      example: <Canvas of={ButtonStories.DontDangerForContinue} />,
    },
  ]}
/>
