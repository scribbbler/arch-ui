// Docusaurus's Layout-level footer is disabled.
// The site footer is rendered via SiteFooter inside DocItem/Layout and
// directly on the landing page (src/pages/index.tsx).
export default function Footer(): null {
  return null;
}
