export interface SkipNavLabels {
  /** Default link text for the skip navigation link. */
  skipToContent: string;
}

export const DEFAULT_LABELS: SkipNavLabels = {
  skipToContent: 'Skip to main content',
};
